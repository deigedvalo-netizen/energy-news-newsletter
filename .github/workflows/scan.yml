name: scan
# Daily scan -> tag -> cluster -> trends -> digest -> publish site (FR-1..FR-16, NFR-1). No LLM calls. Optional news API keys come from secrets.
on:
  schedule:
    - cron: "17 11 * * *"  # 06:17 America/Chicago in summer, 05:17 in winter: one UTC time that needs no daylight-saving edit.
                           # Off the top of the hour because GitHub delays those schedules the most.
  workflow_dispatch:
    inputs:
      force_fail:
        description: "Fail on purpose, to check that the failure alert works"
        type: boolean
        default: false
permissions:
  contents: write
  issues: write   # failure alert
concurrency:
  group: site-publish
  cancel-in-progress: false
jobs:
  scan:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Self-test of the failure alert
        if: inputs.force_fail
        run: exit 1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: pip install -e ".[dev]"
      - name: Unit and acceptance tests
        run: pytest -q -p no:warnings
      - name: Restore database from data branch
        run: |
          mkdir -p data
          if git ls-remote --exit-code --heads origin data >/dev/null; then
            git fetch --depth=1 origin data
            git show origin/data:energynews.db > data/energynews.db
          fi
      - name: Restore current site from gh-pages
        run: |
          mkdir -p site
          if git ls-remote --exit-code --heads origin gh-pages >/dev/null; then
            git fetch --depth=1 origin gh-pages
            git archive origin/gh-pages | tar -x -C site
          fi
      - name: Run pipeline
        env:
          GUARDIAN_API_KEY: ${{ secrets.GUARDIAN_API_KEY }}
          NEWSDATA_API_KEY: ${{ secrets.NEWSDATA_API_KEY }}
        run: python -m energynews.run --db data/energynews.db --site site --registry config/sources.yaml --settings config/settings.yaml --organizations config/organizations.yaml
      - name: Re-apply ledger and published issues
        run: python -m energynews.issues.gate --issues issues --site site --settings config/settings.yaml --organizations config/organizations.yaml
      - name: Publish site atomically (single-commit gh-pages, NFR-6/7)
        run: |
          cd site
          git init -q -b gh-pages
          git add -A
          git -c user.name="energynews-bot" -c user.email="41898282+github-actions[bot]@users.noreply.github.com" commit -qm "site: scan $(date -u +%FT%TZ)"
          git push -f "https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}.git" gh-pages
      - name: Save database (single-commit data branch)
        run: |
          mkdir -p /tmp/databranch && cp data/energynews.db /tmp/databranch/
          cd /tmp/databranch
          git init -q -b data
          git add energynews.db
          git -c user.name="energynews-bot" -c user.email="41898282+github-actions[bot]@users.noreply.github.com" commit -qm "data: $(date -u +%FT%TZ)"
          git push -f "https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}.git" data
      - name: Open an issue if this run failed
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          title="${{ github.workflow }} run failed on $(TZ=America/Chicago date +%F)"
          found=$(gh issue list --state open --search "$title in:title" --json number --jq '.[0].number // empty')
          body="The **${{ github.workflow }}** workflow failed ($(TZ=America/Chicago date '+%F %H:%M %Z'), trigger: ${{ github.event_name }}).

          Log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

          The site keeps serving the last good version until this is fixed. Close this issue once the next run succeeds."
          if [ -n "$found" ]; then gh issue comment "$found" --body "Failed again: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}";
          else gh issue create --title "$title" --body "$body"; fi
